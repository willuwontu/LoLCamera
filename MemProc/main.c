#include "MemProc.h"

int main()
{

	/*
	float a = 1000.0;
	char *mask = "xxxx";
	unsigned char pattern[200];
	memcpy(pattern, &a, 4);

	for (int i = 0; i < 4; i++)
	{
		printf("%.2x ", pattern[i]);
	}

	printf("\n");

	//MemProc *mp = memproc_new(get_pid_by_name("SimpleLoopValue.exe"), 4);
	*/

    return 0;
}
